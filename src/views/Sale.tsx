import SalePanel from "components/sale/salePanel";
import { Container } from "react-bootstrap";

const Sale = () => {
  return (
    <Container>
      <SalePanel />
    </Container>
  );
};

export default Sale;
